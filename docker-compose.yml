services:
  leo-security:
    build: .
    container_name: leo-security-dashboard
    ports:
      - "5173:5173"
      - "3001:3001"
    volumes:
      - .:/app
      - /app/dashboard/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
